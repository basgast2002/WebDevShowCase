using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace testProject
{
    [TestClass]
    public class PayoutCalculatorTests
    { /*
        #region Public Methods

        private string[] _images = {
            "/Images/Cursed_Coins_Gem.png",
            "/Images/pirate-ship.png",
            "/Images/Cursed_Coins_Curse.png",
            "/Images/storm.png",
            "/Images/fish.png",
            "/Images/chicken-leg.png",
            "/Images/dollar.png"
        };

        private GameController gameController;

        public PayoutCalculatorTests(SignInManager<ApplicationUser> signInManager, UserManager<ApplicationUser> userManager)
        {
            gameController = new(signInManager, userManager);
        }

        [TestMethod]
        public void CalculatePayout_ReturnsCorrectPayout_WhenAllImagesMatch()
        {
            // Arrange

            string img1 = _images[0];
            string img2 = _images[0];
            string img3 = _images[0];

            // Act
            int payout = gameController.CalculatePayout(img1, img2, img3);

            // Assert
            Assert.That(1000, Is.EqualTo(payout));
        }

        [Test]
        public void CalculatePayout_ReturnsCorrectPayout_WhenDifferentImagesMatch()
        {
            // Arrange
            string img1 = _images[0];
            string img2 = _images[6];
            string img3 = _images[6];

            // Act
            int payout = gameController.CalculatePayout(img1, img2, img3);

            // Assert
            Assert.That(2, Is.EqualTo(payout));
        }

        [Test]
        public void CalculatePayout_ReturnsZero_WhenNoMatchingImages()
        {
            // Arrange
            string img1 = _images[0];
            string img2 = _images[1];
            string img3 = _images[2];

            // Act
            int payout = gameController.CalculatePayout(img1, img2, img3);

            // Assert
            Assert.That(0, Is.EqualTo(payout));
        }

        #endregion Public Methods

    */
    }
}