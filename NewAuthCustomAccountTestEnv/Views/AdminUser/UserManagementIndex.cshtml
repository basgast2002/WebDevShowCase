@model IEnumerable<NewAuthCustomAccountTestEnv.Models.UserModel>

@{
    ViewData["Title"] = "UserManagementIndex";
}

<h1>UserManagementIndex</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coins)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsAdmin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Failedloginattempts)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Coins)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsAdmin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Failedloginattempts)
            </td>
            <td>
                <!--
                    <form method="post" asp-controller="AdminUser" asp-action="UserManagerEdit">

                        <input type="hidden" name="id" value="@item.Id" />

                        <button id="submitbutton" type="submit" >edit</button>
                    </form>
                    -->
                    <a method= "post" asp-action="UserManagerEdit" asp-controller="AdminUser" data="@item.Id" >Edit</a> | 
                    <a asp-action="Details" asp-area="Identity/Admin/Users" asp-controller="AdminUser" asp-route-id="@item.Username">Details</a> |
                    <a asp-action="Delete" asp-area="Identity/Admin/Users" asp-controller="AdminUser" asp-route-id="@item.Username">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
